# MacPorts Installer addition on 2011-07-13_at_18:27:36: adding an appropriate PATH variable for use with MacPorts.
export MACPORT_PATH=/opt/local/bin:/opt/local/sbin:/opt/local/lib/mysql5/bin
# Finished adapting your PATH environment variable for use with MacPorts.

# command prompt
# export PS1="\e[0;32m\h:\W \u\e[m\$ "
export PS1="\[\033[0;32m\]\h\[\033[00m\]:\[\033[0;32m\]\W \[\033[00m\]$ "

alias ls="ls -G"
alias rhino='java org.mozilla.javascript.tools.shell.Main'
alias clj=clj-env-dir
alias redisstart="redis-server /opt/local/etc/redis.conf"
alias mongostart="sudo mongod -f /opt/local/etc/mongodb/mongod.conf"
mongostop_func () {

# local mongopid=`ps -o pid,command -ax | grep mongod | awk '!/awk/ && !/grep/{print $1}'`;
# just find a simpler way
    local mongopid=`less /opt/local/var/db/mongodb_data/mongod.lock`;
    if [[ $mongopid =~ [[:digit:]] ]]; then
        sudo kill -15 $mongopid;
        echo mongod process $mongopid terminated;
    else
        echo mongo process $mongopid not exist;
    fi
}
alias mongostop="mongostop_func"

# source bash
source ~/git-completion.bash

# nodejs
export NODE_PATH=$HOME/local/node/bin
# java classpath
export CLASSPATH=$CLASSPATH:/opt/local/share/java/bsh.jar
# android
export ANDROID_PATH=$HOME/Programming/android/android-sdk-mac_x86/platform-tools
# scala
export SCALA_PATH=$HOME/Programming/scala/scala/bin
# clojure
export CLOJURE_EXT=~/.clojure
export CLOJURE_PATH=/Users/kev/Programming/clojure/clojure-contrib/launchers/bash
export LEIN_PATH=/Users/kev/Programming/clojure/leiningen/

# my bash script
export BASHSCRIPT=/Users/kev/bin/bash

export USRLOCAL_PATH=/usr/local/bin

export PATH=$MACPORT_PATH:$ANDROID_PATH:$CLOJURE_PATH:$LEIN_PATH:$CLOJURESCRIPT_PATH:$GO_PATH:$BASHSCRIPT:$USRLOCAL_PATH:$PATH
export PATH=$PATH:/opt/node/bin
export NODE_PATH=/opt/node:/opt/node/lib/node_modules
