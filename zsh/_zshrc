ZSH=$HOME/.oh-my-zsh
ZSH_THEME="mh"
ZSH_THEME="bunnyruni"
plugins=(docker docker-compose git git-extras git-flow git-hubflow git-remote-branch python safe-paste)

source $ZSH/oh-my-zsh.sh

# User configuration

alias v='vim'

alias mt='make test'
alias mtv='make test verbosity=2'
alias pm='python manage.py'
alias pms='pm shell'
alias pmrs='pm runserver'
alias pmrsp='pm runserver_plus'
alias pmsdb='pm syncdb'
alias pmm='pm migrate'
alias pmmm='pm makemigrations'
alias pmml='pm migrate --list'
alias pmsm='pm schemamigration'
alias pmsu='pm createsuperuser'

alias mkv='mkvirtualenv'

alias py='python'
alias py3='python3'

alias dc='docker-compose'
alias dcr='docker-compose run --rm'
alias dcrp='dcr padrino'
alias p='dcr padrino bundle exec padrino'
alias pc='dcr padrino bundle exec padrino console'
alias pct='dcr -e RACK_ENV=test padrino bundle exec padrino console'
alias pr='dcr padrino bundle exec padrino rake'
alias cleantest='dcr -e RACK_ENV=test padrino bundle exec padrino rake ar:reset'
alias drspec='dcr -e RACK_ENV=test padrino bundle exec rspec'
alias dgulp='docker-compose run --rm -w /growkudos/client padrino ../node_modules/.bin/gulp'
alias dkarma='docker-compose run --rm -w /growkudos/client padrino ../node_modules/.bin/gulp karma'
alias code_deoderant='git show --name-only|grep .rb$|xargs rubocop'

alias emacs='/Applications/Emacs.app/Contents/MacOS/Emacs -nw'


export PATH=$HOME/bin:/usr/local/bin:$PATH
export PATH=$HOME/.roswell/bin:$PATH
# export MANPATH="/usr/local/man:$MANPATH"

export EDITOR='vim'

#export WORKON_HOME=$HOME/.envs
#export PROJECT_HOME=$HOME/code/python/
#export VIRTUALENVWRAPPER_SCRIPT=/usr/local/bin/virtualenvwrapper.sh
#source /usr/local/bin/virtualenvwrapper.sh

# Set colors to match iTerm2 Terminal Colors
export TERM=xterm-256color tmux

# Compilation flags
export CPPFLAGS=-Qunused-arguments
export CFLAGS=-Qunused-arguments

export GOPATH="$HOME/go"
export GOBIN="$HOME/go/bin"
PATH=$PATH:$GOBIN
export PATH="$HOME/.rbenv/bin:$PATH"
export PATH=$PATH:/Applications/Postgres.app/Contents/Versions/latest/bin
export PATH=$PATH:$HOME/go/bin

# eval "$(rbenv init -)"

# added by travis gem
[ -f /Users/kevin/.travis/travis.sh ] && source /Users/kevin/.travis/travis.sh

### Added by the Heroku Toolbelt
export PATH="/usr/local/heroku/bin:$PATH"
export PATH="/usr/local/sbin:$PATH"

eval "$(direnv hook zsh)"
eval "$(scmpuff init -s)"

# The next line updates PATH for the Google Cloud SDK.
if [ -f '/Users/k/google-cloud-sdk/path.zsh.inc' ]; then source '/Users/k/google-cloud-sdk/path.zsh.inc'; fi

# The next line enables shell command completion for gcloud.
if [ -f '/Users/k/google-cloud-sdk/completion.zsh.inc' ]; then source '/Users/k/google-cloud-sdk/completion.zsh.inc'; fi
